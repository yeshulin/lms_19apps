<?php


namespace frontend\controllers\api;

use Yii;
use frontend\models\Organ;
use frontend\models\Member;
use yii\filters\AccessControl;
use frontend\models\Search\ContentSearch;
use yii\web\NotFoundHttpException;
use yii\filters\VerbFilter;

/**
 * OrganController implements the CRUD actions for Organ model.
 */
class OrganController extends CurdController
{
    public $enableCsrfValidation = false;//关闭csrf验证
    public $orderField=[
        'id'  , 'userid' , 'order'  , 'inputtime', 'updatetime',
        //'videoPath', 'content', 'status'
    ];
    public $searchField=[
        'title'  , 'catid' , 'keywords'  , 'description', 'username','content'
        //'videoPath', 'content', 'status'
    ];
    public $modelName="Organ";
    public $searchModelName="OrganSearch";
    public $namespace="\\frontend\\models\\";
    public $action=['list',"view",'create','update','user'];//允许的操作
    public $returnField=['*'];
    /**
     * @inheritdoc
     */
    public function behaviors()
    {
        return [
//            'access' => [
//                'class' => AccessControl::className(),
//                'rules' => [
//                    [
//                        'actions' => ['list','view'],
//                        'allow' => true,
//                    ],
//                ],
//            ],
            'verbs' => [
                'class' => VerbFilter::className(),
                'actions' => [
                    'delete' => ['POST'],
                ],
            ],
        ];
    }
    public function  init(){
        parent::init();
    }
    public function _create($model, $namespace = "\\frontend\\models\\")
    {
        $this->guestQuit();
        $this->getUid();
        parent::_create($model, $namespace); // TODO: Change the autogenerated stub
    }
    public function _update($model, $namespace = "\\frontend\\models\\")
    {
        $this->guestQuit();
        $this->getUid();
        parent::_update($model, $namespace); // TODO: Change the autogenerated stub
    }

    /*
     * 查询用户认证信息
     * */
    public function actionUser(){
        if (!Yii::$app->request->isPost) {
            if(isset($this->queryParams['userid'])){
                $this->rawBody['userid']=intval($this->queryParams['userid']);
            }
        }
        if(isset($this->rawBody['userid']) && $this->rawBody['userid'] != '') {
            $data = Organ::findByUserid($this->rawBody['userid']);
            $this->setReturn('', '', $data);
        }else{
            $this->setReturn("0003", "failed", '', "缺少参数");
        }
    }
    /**
     * Finds the Content model based on its primary key value.
     * If the model is not found, a 404 HTTP exception will be thrown.
     * @param string $id
     * @return Content the loaded model
     * @throws NotFoundHttpException if the model cannot be found
     */
//    protected function findContent($id)
//    {
//        return $this->findModel($id,true,"Content","\\backend\\models");
//    }
}